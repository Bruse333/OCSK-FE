# 2026-08-04 工作日志

## 项目结构通读（OCSKILL-FE）
- 用户要求通读 PROJECT_CONTEXT.md 并核对源码，为后续前端需求做准备。
- 已核对：router/index.js、utils/request.js、utils/token.js、main.js、SystemLayout.vue、Retrieval.vue（部分），文档与代码一致。
- 关键约定已沉淀到 MEMORY.md。

## 故障排查流程构建器方案（设计稿，未写代码）
- 用户提出需求：网页拖拽搭建故障排查流程（思维导图式），JSON 导入导出，检索详情可切换"流程模式"引导排查。要求先出方案。
- 已输出完整方案到 docs/故障排查流程构建器设计方案.md。
- 核心设计：4 种节点 start/step/decision/end；跳转全部由 edges[] 表达（decision 出边带 sourceOption），节点不存目标 id，画布与 Runner 共用一份数据；Runner 为状态机 + history 栈回退。
- 技术选型建议 @vue-flow/core；存储分阶段：P1 纯 JSON 文件（零后端改动，备选：JSON 以 .flow.json 附件传 OSS 存进 trbsts 的 doc[]）；P2 Runner；P3 后端 t_flow 表。
- 新路由规划 /system/flow-builder（PC 优先，privilege>=2），Runner 全员可用（含 H5）。
- 等用户确认方案后再动工。

## P1 流程构建器开发完成（构建验证通过）
- 依赖：@vue-flow/core 1.48.2 + background + controls；peer 要求 vue>=3.3，已将 vue 升到 3.5.40（package.json ^3.3.0），plugin-vue 3 + vite 3 下 npm run build 通过。
- 新发现的项目基建：src/utils/uploadQueue.js（全局上传串行队列+200ms 节流，uploadFile(file)→OSS url）、src/utils/file.js（deleteFiles 清 OSS）。
- 新增：src/utils/flowSchema.js、src/system/FlowBuilder.vue、src/system/flow/FlowNode.vue、src/system/flow/FlowRunner.vue。
- 改动：router 加 /system/flow-builder + 守卫 privilege>=2；SystemLayout 菜单"流程构建器" requiredPrivilege:2；PROJECT_CONTEXT.md 补 8.9 节。
- 关键决策：业务数据存 VF node.data，buildFlow() 序列化；start 节点 deletable:false 且禁拖入第二个；decision 每选项一个 source Handle（id=option id），每选项仅一条出边；导入 regenerateIds；草稿 localStorage ocskill_flow_draft 防抖自动保存恢复。
- P2 待做：检索详情加"流程模式"Tab（复用 FlowRunner），.flow.json 走 /upload 存 OSS 并约定存进 trbsts 的 doc[]（用户已确认，后续数据库加正式字段后再改前端）。
